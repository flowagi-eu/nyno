from mistralai import Mistral

# Initialize client (example, may require API key depending on usage)
client = Mistral()

# Example usage (replace with your model/method)
response = client.generate("Hello, world!")
print(response)

